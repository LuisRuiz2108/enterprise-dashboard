// REST Countries v5 requires an API key (Authorization: Bearer <key>).
// Get one at https://restcountries.com (free plan).
//
// ⚠️ In a client-side Angular app this key ships in the browser bundle and is
// therefore public. Use a key restricted to your domain, and consider adding
// this file to .gitignore if the key should not be committed.
export const environment = {
  restCountriesApiKey: 'rc_live_f8cbe9c7c87d4b7ca562d0905f9320e2',
};
